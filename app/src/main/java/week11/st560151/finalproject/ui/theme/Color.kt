package week11.st560151.finalproject.ui.theme

import androidx.compose.ui.graphics.Color

// Brand green (landing background, accents, success states)
val BrandGreen = Color(0xFF8FE388)
val BrandGreenLight = Color(0xFFDFF6D8)
val BrandGreenDark = Color(0xFF4CAF6D)

// Brand ink (primary buttons/text)
val BrandBlack = Color(0xFF1A1A1A)
val BrandBlackSoft = Color(0xFF2B2B2B)

val BackgroundLight = Color(0xFFFFFFF8)
val SurfaceLight = Color(0xFFFFFFFF)
val OutlineLight = Color(0xFF84A354)

val BackgroundDark = Color(0xFF121412)
val SurfaceDark = Color(0xFF1E211E)

val ErrorRed = Color(0xFFD3455B)

// Link/accent color used for "Sign in" / "Forgot password?" style inline links.
val LinkGreen = Color(0xFF84A354)

// AddExpense chip selection colors (category vs. paid-by/participant).
val CategorySelected = Color(0xFFC97B5F)
val ParticipantSelected = Color(0xFFB7EFA4)

// Standard "box" style used across cards throughout the app.
val CardBackground = Color(0xFFFFFFFF)
val CardBorder = Color(0xFF969696)

// Bottom navigation bar.
val BottomNavBackground = Color(0xFFFFFFFE)
val BottomNavBorder = Color(0xFFF2F2EC)

// Floating "+" action buttons.
val FabGreen = Color(0xFFCBF587)

// Group type / category tag pill (e.g. "Roommates").
val TagBackground = Color(0xFFEEEEEE)
val TagText = Color(0xFF737373)

// Thin separator for line-style lists (e.g. GroupExpand's Activity feed).
val DividerGray = Color(0xFFE5E5E5)

// Settlement flow: biometric pulse circle / confirmed waiting circle.
val BiometricCircle = Color(0xFFCBF587)
val ConfirmedCircle = Color(0xFFEBEBEB)
